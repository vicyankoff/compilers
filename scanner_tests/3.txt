# Can you scanner handle comments at the
# beginning of the program?

program testcomments;  # This test case will check
# your handling of comments.
# Consecutive comments can be tricky.
	foo:int;

	bar  # Can your scanner handle
	:#comments between
	int;	# tokens?

	# this comment is preceeded by a tab
  # this comment is preceeded by spaces
begin 
	# trupl comments don't nest # so this should be treated like one comment.
	foo # foo
	:=# is assigned
	bar#bar
	;# semicolon
end;#comments at the end can be tricky to deal with as well